{
  "name": "bemata-static-site",
  "private": true,
  "description": "Statički sajt — na Vercelu postavi Root Directory na koren ovog repozitorijuma (.)",
  "scripts": {
    "build": "npm run build:menu-embed && node -e \"require('fs').accessSync('index.html'); console.log('OK: static root');\"",
    "build:menu-embed": "npm --prefix menu-book-demo run build:embed"
  },
  "dependencies": {
    "gsap": "^3.15.0",
    "three": "^0.184.0"
  }
}
